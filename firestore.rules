rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm giriş yapmış kullanıcıların (Admin/Müşteri) okuma ve yazma işlemlerine izin ver
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}